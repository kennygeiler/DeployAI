server {
    listen 2020;
    server_name _;

    location = /health {
        return 200 '{"status":"ok","service":"freetsa-stub"}\n';
        add_header Content-Type application/json;
    }

    location / {
        return 200 'freetsa-stub: Story 1.13 placeholder. Any POST returns 200.\n';
        add_header Content-Type text/plain;
    }
}
