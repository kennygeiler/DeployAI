# Deploy guardrail — replicas vs memory queues

**PS-Q-104:** one `apps/web` replica when queues are in-memory; enable CP backend before scaling out ([queue-durability-modes.md](../pilot/queue-durability-modes.md)).
