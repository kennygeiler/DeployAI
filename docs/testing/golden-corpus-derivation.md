# Golden corpus derivation (Story 4-4, NFR50)

The `gen-*.yaml` queries under `tests/golden/queries/` are **synthetic** placeholders generated by `pnpm run golden:expand-corpus` so the replay-parity harness, validators, and CI have ≥200 files with correct schema and 21-cell matrix coverage.

**High-confidence expected IDs:** The first 50 generated queries (by ID order) use random UUIDs for `expected_citations` suitable for **rule-based** development tests only. Replace with node IDs from the **seeded canonical-memory tenant** (Story 1.7 local stack) when wiring real evaluators.

**Domain SME work:** As noted in `epics.md` Story 4-4, replacing synthetic text and UUIDs with mission-real queries typically needs **1.5–2 weeks** calendar with subject-matter input. This document should accumulate references to workshops, data sources, and sign-off as the corpus hardens.
