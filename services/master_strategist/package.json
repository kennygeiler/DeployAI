{
  "name": "@deployai/master-strategist",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "uv sync --frozen",
    "lint": "uv run ruff check src tests && uv run ruff format --check src tests",
    "typecheck": "uv run mypy",
    "test": "uv run pytest",
    "test:cov": "uv run coverage erase 2>/dev/null || true; uv run coverage run -m pytest -q; uv run coverage report --include='src/master_strategist/*'",
    "clean": "rm -rf .venv .pytest_cache .mypy_cache .ruff_cache"
  }
}
