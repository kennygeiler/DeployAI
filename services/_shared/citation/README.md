# deployai-citation

Pydantic models mirroring `packages/contracts` (citation envelope v0.1.0). Published as a path dependency for `deployai-control-plane` and other Python services.

Import:

```python
from deployai_citation import CitationEnvelopeV01
```
