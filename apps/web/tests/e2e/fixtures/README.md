# E2E fixtures

`pilot-access-e2e-*.pem` — **test-only** RSA key used to mint JWTs in Playwright for Story 15.1. Do not use in production; configure `DEPLOYAI_WEB_JWT_PUBLIC_KEY_PEM` with your control-plane public key instead.
