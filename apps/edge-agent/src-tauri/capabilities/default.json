{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window/app/event defaults for the main shell. Story 11.1: add domain capabilities (fs, dialog, audio, keychain, http) as separate identifiers; keep this file limited to core:default.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
