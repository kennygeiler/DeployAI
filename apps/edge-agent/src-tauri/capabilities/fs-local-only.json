{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "fs:local-only",
  "description": "App-local filesystem access only ($APPLOCALDATA / app support). No arbitrary path or home-directory grants (Story 11.1 AR17).",
  "windows": ["main"],
  "permissions": [
    "fs:allow-applocaldata-meta-recursive",
    "fs:allow-applocaldata-read-recursive",
    "fs:allow-applocaldata-write-recursive"
  ]
}
