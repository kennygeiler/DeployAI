{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog:file-select",
  "description": "Native open/save dialogs for explicit user-chosen paths only (Story 11.1).",
  "windows": ["main"],
  "permissions": ["dialog:allow-open", "dialog:allow-save"]
}
