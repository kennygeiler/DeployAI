# Story 6-7 — Phase transition proposals (done)

**Package:** `phase_propose.py` — `PhaseTransitionProposalBundle`, `build_control_plane_propose_body()`, `build_phase_transition_bundle()` (validates `can_transition`), `should_propose_phase_transition` heuristic. Evidence carries `CitationEnvelopeV01` alongside UUIDs for API `evidence_event_ids`.
